{
  "$schema": "agentmaurice.workflow/v2",
  "schema_version": 2,
  "workflow_id": "existing-customer",
  "kind": "workflow",
  "title": "Récapitulatif existing",
  "version": "1",
  "actions": [],
  "output": {
    "id": "{{ prospect.id }}",
    "classification": "existing"
  },
  "output_schema": {
    "type": "object",
    "properties": {
      "id": {
        "type": "string"
      },
      "classification": {
        "enum": [
          "new",
          "existing",
          "review"
        ]
      }
    },
    "required": [
      "id",
      "classification"
    ],
    "additionalProperties": false
  }
}

interface:
  display_name: "AgentMaurice"
  short_description: "Author and deploy governed Agent Specs"
  default_prompt: "Use $agentmaurice to make this change through the Git-native Agent Spec V2 check, plan, human approval, apply, and verify workflow."

policy:
  allow_implicit_invocation: true

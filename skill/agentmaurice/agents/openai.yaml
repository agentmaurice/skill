interface:
  display_name: "AgentMaurice"
  short_description: "Build and operate AgentMaurice apps through MCP or CLI"
  default_prompt: "Use $agentmaurice to turn an app idea into a deployed AgentMaurice app or backend, choose a frontend starter if needed, verify it, and explain how to access it."

policy:
  allow_implicit_invocation: true
